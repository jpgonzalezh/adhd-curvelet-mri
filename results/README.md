# Results

Output CSVs are written here by the analysis scripts (path configurable via the
`RESULTS_ROOT` environment variable). This directory is git-ignored except for
this file. Each script prints a summary to stdout and writes per-fold results,
rank-stability tables, and confusion matrices used to build the manuscript
tables and supplementary material.
